https://www.codeupstart.com/project/build-slack-with-react/

```
sudo apt-get install npm
sudo npm install -g browserify watchify 
sudo npm install reactify
sudo npm install react react-dom
```

---

```
$ browserify -t reactify components/chat.js -o bundle.js
$ watchify -t reactify components/chat.js -o bundle.js -v
```

http://www.codeinstallguide.com/

Step 1 - Install Python

Step 2 - Install Pip (Optional)

Step 3 - Install Virtualenv

`$ virtualenv venv`

`$ . venv/bin/activate`

`$ pip install Flask`

http://flask.pocoo.org/

---

Pusher
https://pusher.com/

pip install pusher

https://pusher.com/docs/client_api_guide/client_presence_channels
